{
  "name": "gridpaw",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist --project-name gridpaw --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
